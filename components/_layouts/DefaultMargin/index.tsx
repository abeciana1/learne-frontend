
const DefaultMargin = ( { children }:{ children:any } ) => {

    return (
        <section
            className="mx-20"
        >
            {children}
        </section>
    )
}

export default DefaultMargin