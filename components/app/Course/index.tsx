import cx from 'classnames'

interface CourseI {
    name: string;
    image: string;
    description: string;
    price: number;
    href: string;
}

const Course = ({
    name,
    image,
    description,
    price,
    href
}: CourseI) => {
    return (
        <div>
            
        </div>
    ) 
}